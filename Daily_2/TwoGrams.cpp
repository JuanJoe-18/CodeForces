#include <bits/stdc++.h>
#include <map>
#include <string>
using namespace std;
typedef long long ll;
int main() {
  string s;
  cin >> s;
  int n = s.size();
  int l = 0, r = 0;
  map<string, int> f;
  while (l < n) {
    while (r < n && s[r] > s[l]) {
        

    }
  }

  return 0;
}