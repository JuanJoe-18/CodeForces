#include <bits/stdc++.h>
using namespace std;
int main(){




    

    return 0;
}

/*int l = 0, r = 0;
long long ans = 0, sum = 0;
while (l < n) {
    // 1️⃣ Expandir mientras se pueda
    while (r < n && can_add(r)) {
        add(r);
        r++;
    }

    // 2️⃣ Actualizar respuesta
    ans = max(ans, r - l); // o min, o sum, depende del problema

    // 3️⃣ Reducir desde la izquierda
    remove(l); // usualmente sum -= a[l];
    l++;
}
cout << ans << endl;*/