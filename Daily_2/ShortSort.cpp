#include<bits/stdc++.h>
#include <string>
using namespace std;
string swaped( string s);
int main(){
    int t;
    cin >> t;
    while (t--) {
        string s;
        cin >> s;
        if (swaped(s).size() ==3);
    }
    return 0;
}

string swaped( string s){
    string swap;
    int count = 0;
    for (int i = 1; i < s.size(); i++) {
        if (s[i-1] ) {
        
        }

        
    }





    return swap;
}