#include <bits/stdc++.h>
#include <map>
#include <memory>
using namespace std;
map<int, int> f_primos;
typedef long long ll;

int main() {
  int n;
  cin >> n;
  vector<int> dict;

  vector<int> a(n);
  for (int i = 0; i < n; i++) {
    cin >> a[i];
  }
}
